AI_PROVIDER=ollama
AI_MODEL=qwen3:8b

# Telegram notifications for VPS cron alerts.
# Create a bot with BotFather, then put the token/chat ID on the VPS only.
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Never commit real exchange keys. V1 uses paper mode only.
EXCHANGE_API_KEY=
EXCHANGE_API_SECRET=
